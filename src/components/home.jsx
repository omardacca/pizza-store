import React, { Fragment } from "react";
import { useState, useEffect } from "react";
import TitlebarGridList from './titleBarGridList';
import ScrollableTabsButtonForce from './common/scrollableTabsButtonForce';

function Home(props) {
  useEffect(() => {
    
  }, [])
  

  return (
    <Fragment>
      <ScrollableTabsButtonForce />
    </Fragment>
  )
}

export default Home;